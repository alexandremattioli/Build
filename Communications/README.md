# Communications

This directory collects reference material for the CloudStack/CCC2025 coordination protocol.  Use `./scripts/send_message.sh`, `read_messages.sh`, and `autoresponder.sh` as documented in `docs/CCC2025_PLAN.md`.
